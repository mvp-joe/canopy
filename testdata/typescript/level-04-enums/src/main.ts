enum Color {
  Red = "RED",
  Green = "GREEN",
  Blue = "BLUE",
}

enum Priority {
  Low,
  Medium,
  High,
}

function colorName(c: Color): string {
  return "unknown";
}

const primary: Color = Color.Red;
